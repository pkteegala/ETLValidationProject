name: ETLValidationProject
version: '1.0'
profile: ETLValidationProject
config-version: 2

model-paths: ["models"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_modules"

models:
  ETLValidationProject:
    staging:
      +materialized: table
    marts:
      +materialized: table